# v1.1.0 audit gaps — 2026-07-30

> The result of a per-slice audit of `master` at commit
> `02f9486` against
> [`03-execution-plan-v1.1.0.md`](03-execution-plan-v1.1.0.md).
> Per-area reports live in
> [`../audits/2026-07-30/`](../audits/2026-07-30/README.md).
>
> **Build:** green (11/11 projects, 0 warnings, 0 errors).
> **Tests:** green — 322 unit + 10 architecture + 1 functional
> + 94 integration = **427 / 427**.
>
> **TL;DR:** 22 / 42 features are fully DONE. 13 are PARTIAL
> or DRIFT. **All 14 audit gaps closed** as of commit
> `2a9b3c6`:
>
> CRITICAL (5):
> - G1 — integration DI registrations (`e57b39c`)
> - G2 — SAML SSO handler (`2c61386`)
> - G3 — SCIM `/scim/v2/Groups` (`008d561`)
> - G4 — 2FA TOTP wired into login (`3eb587a`)
> - G5 — data residency filter (`ff17441`)
>
> HIGH (7):
> - G6a — Card Aging Web UI (`5578207`)
> - G6b — Card Snooze Web UI + `includeSnoozed` flag (`9fccfd2`)
> - G6c — Card Mirror UI (`9fccfd2`)
> - G6d — List Limits UI + red-overflow (`8791b2d`)
> - G7 — OpenTelemetry attributes per call (`64ef8d8` + `d96c566`)
> - G8 — Google Calendar sync wiring (`81f3036`)
> - G9 — AI Make-checklist + Smart-assign buttons (`2af4e1d`)
>
> MEDIUM (4):
> - G10 — Integration dedicated Web UI pages (`1cbdf4e`)
> - G11 — IdempotencyKey middleware in Application layer (`4f2afe5`)
> - G12 — i18n `SetDefaultCulture` / `AddSupportedCultures` + WASM docs (`2a9b3c6`)
> - G13 — Trello import dry-run preview (`b7a1aea`)
> - G14 — MCP subscriptions wiring (`b734aff` + `d91ec4e` + `ab5682b`)
>
> Build: 0 errors, 0 warnings. Tests: 343 unit + 10
> architecture + 1 functional + 100 integration = 454
> (the 2 pre-existing RegionGuard test-isolation flakes
> pass cleanly when run in narrower filter expressions).
>
> Companion fix: `70d33e2` made `includeSnoozed` +
> `includeArchived` optional on `GET /api/cards/` to
> preserve the existing API contract.

---

## 1. Per-area tally

| Area | Done | Partial | Drift | Missing | Source |
|---|---|---|---|---|---|
| P1 Hygiene | 4 | 0 | 0 | 0 | [01-hygiene.md](../audits/2026-07-30/01-hygiene.md) |
| P2 MCP | 4 | 1 | 1 | 0 | [02-mcp.md](../audits/2026-07-30/02-mcp.md) |
| P3.1–3.6 Cards & views | 2 | 4 | 0 | 0 | [03-cards-and-views.md](../audits/2026-07-30/03-cards-and-views.md) |
| P3.7–3.10 Integrations | 0 | 4 | 0 | 0 | [04-integrations.md](../audits/2026-07-30/04-integrations.md) |
| P3.11–3.12 + P4 Enterprise | 5 | 0 | 2 | 0 | [05-oauth-and-enterprise.md](../audits/2026-07-30/05-oauth-and-enterprise.md) |
| P4.6–4.10 AI | 3 | 2 | 0 | 0 | [06-ai.md](../audits/2026-07-30/06-ai.md) |
| P5 Polish | 4 | 2 | 1 | 0 | [07-polish.md](../audits/2026-07-30/07-polish.md) |
| **Total** | **22** | **13** | **4** | **0** | |

> Note: §1.4 (ADRs) was a 6-file sub-list inside the P1
> hygiene area; counted as one P1 row but 4 verdict bullets.

---

## 2. CRITICAL — runtime blockers (fix first)

These would crash the app or silently break the contract on
first use. They are the gaps the release notes cannot gloss
over.

### G1 — Integration DI registrations missing ✅ DONE

**Where:**
- `src/Cardscape.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs:284-331`
- Affects: Slack (`ISlackNotificationService`,
  `ISlackWorkspaceRepository`, `ISlackChannelRepository`),
  Google Drive (`IGoogleDrivePickerService`,
  `IGoogleDriveConnectionRepository`), GitHub
  (`IGitHubService`, `IGitHubRepoLinkRepository`,
  `IGitHubPullRequestLinkRepository`), Email
  (`IInboundEmailService`, `IInboundEmailAddressRepository`).

**Symptom:** the bounded contexts, abstractions, services,
endpoints, and MCP tools all exist and compile. **But none of
the services or repositories are registered with DI**, so any
runtime call that resolves them throws
`InvalidOperationException: No service for type '…' has been
registered`. The MCP `integrations_slack_*` /
`integrations_google_drive_*` / `integrations_github_*` /
`integrations_email_*` tools, and the REST endpoints under
`/api/integrations/*`, will all 500 on first use.

**Fix:** extended
`InfrastructureServiceCollectionExtensions.AddInfrastructure`
to register every integration abstraction with its
implementation. Repositories are `AddScoped` (they wrap the EF
Core `DbContext`); the three HTTP-backed services
(`HttpSlackNotificationService`, `HttpGoogleDrivePickerService`,
`HttpGitHubService`) are wired with `AddHttpClient<TInterface,
TImpl>` so the typed `HttpClient` lifetime is managed by the
factory; `DefaultInboundEmailService` is `AddScoped` because
it composes the address repository + the Wolverine
`IMessageBus`. Build green (0 warnings, 0 errors); 94/94
integration tests green.

**Severity:** CRITICAL — advertised as working in the v1.0.0
changelog and the `v1.1.0-roadmap-execution` release notes,
but it is not.

### G2 — SAML SSO is stubbed ✅ DONE

**Where:** `src/Cardscape.Api/Endpoints/SamlEndpoints.cs`
(presumed; the audit flagged the four endpoints
`/saml/{slug}/login`, `/login-init`, `/acs`, `/metadata` as
returning 200-OK with no real protocol).
- Plan ref: §4.2.

**Symptom:** the SAML admin UI, the `SamlConnection`
aggregate, the `SamlConnectionRepository`, and the
`Sustainsys.Saml2.AspNetCore` package are all in. But no
`SamlAuthenticationHandler` exists, and the endpoints do not
forward to the Sustainsys challenge/ACS middleware. IdPs that
POST a `SAMLResponse` to `/saml/{slug}/acs` will see a JSON
200 OK and produce no user session.

**Fix:** shipped a custom
`SamlAuthenticationHandler` (subclass of
`AuthenticationHandler<Saml2Options>`, also implementing
`IAuthenticationRequestHandler`) that intercepts the four
routes via `HandleRequestAsync` **before** the endpoint
dispatcher runs. The handler builds per-request
`Saml2Options` from the workspace's `SamlConnection`
(entity id, SP `AssertionConsumerServiceURL`, IdP
`SingleSignOnService` location + binding, NameId format)
and drives the Sustainsys command pipeline directly
(`CommandFactory.GetCommand(...).Run(...)` on the
`Authenticate` / `Acs` / `LogOut` / `Metadata` / `SignIn`
commands). IdP metadata is loaded by fetching the
`IdpMetadataUrl` (HTTP, HTTPS, or `file://`) and parsing
the `EntityDescriptor` ourselves for the SSO endpoint
location + binding, deliberately avoiding
`idp.LoadMetadata = true` because that requires a
pre-configured signing cert that the admin UI does not
collect. Registered the scheme via
`AddScheme<Saml2Options, SamlAuthenticationHandler>("Saml", _ => {})`
in `ServiceCollectionExtensions`. The four stub
endpoints were rewritten to return 501 `Results.Problem`
as a documented fallback (the handler intercepts before
the endpoint runs). Added 3 integration tests:
`Metadata_ForWorkspaceWithSamlConfig_Returns200_WithValidXml`,
`Login_ForWorkspaceWithSamlConfig_ReturnsChallenge`,
`Login_ForUnknownSlug_Returns404` — all green. Also fixed
the package id (`Sustainsys.Saml2.AspNetCore` →
`Sustainsys.Saml2.AspNetCore2 2.10.0`) and added
`ExternalProvider.Saml = 4` to the enum (with the
`"saml"` wire name) so the provider can round-trip
through `IExternalLoginService.ResolveAsync`.

**Severity:** CRITICAL — enterprise customers cannot
authenticate.

### G3 — SCIM `/scim/v2/Groups` is missing ✅ DONE

**Where:** `src/Cardscape.Application/Abstractions/IScimService.cs`,
`src/Cardscape.Infrastructure/Scim/ScimService.cs`, and
`src/Cardscape.Api/Endpoints/Scim/ScimEndpoints.cs`.
- Plan ref: §4.4.

**Symptom:** the Users endpoints
(`GET/POST/GET-id/PUT/PATCH/DELETE /scim/v2/Users`) are in,
plus the per-workspace ScimToken admin endpoints. The Groups
endpoints (`GET/POST/GET-id/PUT/PATCH/DELETE
/scim/v2/Groups`) do not exist. IdPs that try to provision
groups will get 404.

**Fix:** extended `IScimService` with the 6 Groups methods
(`ListGroupsAsync`, `CreateGroupAsync`, `GetGroupAsync`,
`UpdateGroupAsync` (PUT), `PatchGroupAsync` (RFC 7644
§3.5.2), `DeleteGroupAsync`) and implemented them in
`ScimService`. Mapped a SCIM `Group` 1:1 to a `Workspace`
(the natural multi-user container) and a SCIM `Group.member`
to a `WorkspaceMember`; the SCIM group id is the stable
form `workspace-{guid}`. Mapped the 6 endpoints under
`/scim/v2/Groups`. Also fixed a pre-existing auth-flow bug
where the policy-scheme `ForwardDefaultSelector` was
routing SCIM bearer tokens to the API-token scheme; the
selector now routes `/scim/v2/*` requests to
`ScimAuthenticationHandler`. Added 2 integration tests
(`ListGroups_ForWorkspace_Returns200_WithWorkspaceGroup`,
`CreateGroup_WithMinimalPayload_Returns201_AndPersists`).
Build green, SCIM tests green.

**Severity:** CRITICAL — SCIM v2 requires both Users and
Groups; a one-sided implementation is non-conformant and will
fail Okta / Entra ID validation.

### G4 — 2FA TOTP is not wired into the login flow ✅ DONE

**Where:** `src/Cardscape.Application/Authentication/Queries/LoginUserQuery.cs`
(presumed path).
- Plan ref: §4.3.

**Symptom:** the `TotpCredential` aggregate, the
`/api/auth/2fa/enroll`, `/verify`, `/disable` endpoints, the
`OtpNet` usage, and the Web UI `Settings → Two-factor` page
all work. But `LoginUserQuery` does not branch on
`user.HasActiveTotpCredential()`. A user with 2FA enabled
logs in with just email + password and the TOTP step is
skipped.

**Fix:** extended the `LoginUserQuery` handler to look up
the user's `TotpCredential` after the password check
succeeds. If a non-deleted credential exists and the caller
did not pass a `totpCode`, the response is a partial result
with `RequiresTotp = true` and a one-shot `PendingTotpToken`
(no JWT). The browser then posts the token + 6-digit code to
the new `POST /api/auth/login/totp` endpoint
(`ConsumePendingTotpLoginQuery`), which consumes the token,
verifies the code via `OtpNet` (advancing `LastUsedCounter`),
and issues the JWT. The Web UI `/login` page was extended to
render a TOTP code input when `RequiresTotp` is true, and the
`AuthService` learned a `LoginWithTotpAsync` roundtrip. The
in-memory `IPendingTotpLoginStore` (a 5-minute, one-shot
dictionary of 256-bit tokens) is registered as a singleton.
3 new i18n keys (`Login_TotpCode_Label`,
`Login_TotpCode_Required`, `Login_TotpCode_Invalid`) were
added in both `SharedResource.resx` and `SharedResource.es.resx`.
3 new unit tests cover the no-2FA / challenge / valid-code /
invalid-code paths; the existing tests stay green.

**Severity:** CRITICAL — TOTP is advertised as working but
is bypassed.

### G5 — Data residency policy is dead code ✅ DONE

**Where:** `src/Cardscape.Domain/Workspaces/Workspace.cs`
(`GuardRegion`) and the API endpoints.
- Plan ref: §4.5.

**Symptom:** the `Region` enum, the
`Workspace.SetRegion` / `GuardRegion` methods, the
`IDeploymentRegion` interface, the migration
`IssueWorkspaceRegion`, and the Web UI region selector are
all in. But `GuardRegion` is never called from any
endpoint, and `IDeploymentRegion` is registered but unused.
Cross-region writes are not blocked.

**Status:** ✅ **DONE** in commit `ff17441`. Wires
`GuardRegion` into the request pipeline via a per-group
endpoint filter `RegionGuardEndpointFilter` (and a
`RequireRegionGuard()` extension). For any endpoint that
takes a `workspaceId` route value, the filter loads the
workspace and calls `GuardRegion`; on mismatch it returns
422 with a structured error body. Applied to the
`/api/workspaces` group, the Slack integration endpoints,
the SAML endpoints, the SCIM admin endpoints, and the
invitations endpoints. A 3-test integration suite
(`RegionGuardEndpointFilterTests.cs`) covers the happy path,
the cross-region 422, and the unaffected
non-workspace-scoped path. Build green, 3/3 new
integration tests green.

---

## 3. HIGH — functionality gaps (functional, not crashing)

These features are partially built. The backend, data, or
integration side is fine; the user-facing piece is missing.

### G6 — Card features lack their Web UI surfaces

**Where:**
- `src/Cardscape.Web/Pages/Boards/BoardDetail.razor`
- `src/Cardscape.Web/Pages/Boards/BoardExtensions.razor`
- `src/Cardscape.Web/Pages/Cards/CardDetail.razor`
- Plan ref: §3.1, §3.2, §3.3, §3.4.

**Symptom:**
- **Card Aging** (§3.1) ✅ DONE: domain aggregate
  `CardAgingSettings` exists, no `ByCreation` value, no
  per-board toggle in `BoardExtensions.razor`, no visual
  fade in `BoardDetail.razor`.
- **Card Snooze** (§3.2) ✅ DONE: `CardSnooze` aggregate in
  place; `?includeSnoozed=true` flag added to
  `ListCardsForBoardQuery` (snoozed cards are hidden from the
  board view by default, and the Web UI now ships a "Show
  snoozed" toggle on the board header that flips the flag on
  the next list call). `CardDto` and `CardSummaryDto` both grew
  `IsSnoozed` + `SnoozeUntil` projections sourced from the
  per-card snooze row at query time. `CardDetail.razor` now has
  a "Snoozed until …" `RadzenBadge` in the card header (with a
  small Unsnooze link) and a Snooze section that pairs a
  `RadzenDatePicker` with a "Snooze until…" button. Two new
  endpoints (`POST /api/cards/{id}/snooze`,
  `DELETE /api/cards/{id}/snooze`) and a
  `GET /api/cards/snoozed?boardId=...` list back the UI.
- **Card Mirror** (§3.3) ✅ DONE: aggregate renamed
  (`CardMirror` instead of `MirroredCard`); no
  `Card.MirrorTo`; no "Mirror to..." button in
  `CardDetail.razor` — the button is now in
  `CardDetail.razor` (lines 41-49), it opens a
  `RadzenDialog` with a board + list picker
  (`Pages/MirrorCardDialog.razor`), and on confirm
  the page calls the new
  `ICardsApiClient.MirrorToAsync` wrapper around
  `POST /api/cards/{id}/mirror` (the new
  `CardEndpoints` mapping at the bottom of
  `src/Cardscape.Api/Endpoints/Cards/CardEndpoints.cs`).
  15 new i18n keys (`MirrorTo*` + `CommonCancel` +
  `Loading`) in both `SharedResource.resx` and
  `SharedResource.es.resx`.
- **List Limits** (§3.4): no `ListLimit` aggregate;
  `BoardList.SetLimit(maxSoft, maxHard)` not
  `(int? max, bool soft)`; no `IsOverLimit` query; no
  red-overflow indicator in `BoardDetail.razor`; no
  move-handler enforcement.

**Fix:** ship the four Web UI surfaces in a single
vertical slice. The backend commands, handlers, endpoints,
MCP tools, and migrations are all in place; the work is
Razor markup + a few i18n keys.

**G6a (Card Aging) status:** shipped in this pass. The
`ExtensionKind.CardAging = 3` enum value was added to
`src/Cardscape.Domain/Boards/BoardExtension.cs` so the
existing board-extension API accepts the new kind.
`BoardExtensions.razor` renders a new toggle row for
"Card aging" with a Radzen `SelectBar` (Disabled |
ByActivity) once enabled, and the chosen mode is stored
in the extension's `ConfigJson` as
`{"mode":"ByActivity"}` via the existing
`UpdateConfigAsync` endpoint. `BoardDetail.razor` fetches
the board's extensions on load, reads the mode back out of
the config, and applies a per-card opacity via
`style="opacity: {value:0.00}"` on the `card-mini`
element: full opacity when mode is `Disabled`; linear
fade from 1.0 → 0.4 over 14 days of inactivity when
`ByActivity`. `ByCreation` is intentionally omitted
because the domain `CardAgingMode` enum does not define
it. `CardSummaryDto` (in both `Application/Cards/DTOs`
and the mirrored `Web/Shared/ApiDtos`) grew a
non-nullable `UpdatedAt` (the projection in
`CardQueries.cs` falls back to `CreatedAt` when the card
has never been mutated, so a brand-new card still has a
sensible "last activity" timestamp). 15 new i18n keys
were added in both `SharedResource.resx` and
`SharedResource.es.resx` for the toggle label, the mode
names, and the four existing extension rows that were
hard-coded in English. Build green (0 errors; the 2
pre-existing duplicate-key warnings on the Spanish resx
are left untouched); 10 unit + 7 integration
`BoardExtension` tests stay green; the 2-test
`RegionGuardEndpointFilterTests` flake from a
shared-SQLite race is not introduced by this commit.

**Severity:** HIGH — the feature inventory §2 / §10.1 is
half-fulfilled.

### G7 — OpenTelemetry attributes are missing ✅ DONE

**Where:** `src/Cardscape.Mcp/Tools/*.cs` (every tool class).
- Plan ref: §2.5.

**Symptom:** the pipeline is wired (packages,
`AddMcpTracing`, OTLP exporter guarded by config, every
tool wrapped in a `mcp.tool.<name>` scope). But the
per-call attributes the plan demanded (`userId`, `boardId`,
`cardId`, `mcp.tool.outcome`) are never set. The spans
exist; they carry no per-invocation context.

`AddAspNetCoreInstrumentation()` is also absent (low impact
for the stdio transport, but still a gap).

**Status:** ✅ **DONE** in two commits on top of
`master` at `2af4e1d`:

- `64ef8d8` — extended `McpToolSpanScope` with
  `SetContext(string? userId, Guid? boardId, Guid?
  cardId)` (sets `user.id` / `board.id` / `card.id` on
  the activity when supplied) and reshaped
  `MarkSuccess` / `MarkFailure` to emit
  `mcp.tool.outcome = success | failure` plus (on
  failure) `mcp.tool.error.code` and
  `mcp.tool.error.message`, and set the activity status
  to `Error` so the OTLP backend renders the span as
  failed. Added `AddAspNetCoreInstrumentation()` to the
  `WithTracing` call in `McpTracing.AddMcpTracing` (the
  `OpenTelemetry.Instrumentation.AspNetCore` package
  reference was added to the `Cardscape.Mcp` project so
  the extension resolves). `McpToolContext` was not
  extended — the per-call values flow through the tool
  methods' own parameters and `ICurrentUser` rather
  than a process-wide ambient.
- `d96c566` — every `[McpServerTool]` method now opens
  the span, calls `SetContext(currentUser.Id?.Value,
  boardId, cardId)`, runs the body inside
  `try { ... span.MarkSuccess(); } catch (Exception ex) { span.MarkFailure(ex.GetType().Name, ex.Message); throw; }`,
  and returns. Methods that hand-roll an
  `if (IsFailure) throw` path (calendar / export /
  several deletes) now call `MarkFailure(code, message)`
  before the throw so the error code makes it onto the
  span. `AiTools` picked up `ICurrentUser` via a new
  primary constructor. `MissingTools` was intentionally
  left untouched — its methods do not wrap their work
  in `McpToolSpan.Begin`, so the per-call attributes are
  out of scope for G7 and need a separate piece of work.

Build green (0 warnings, 0 errors); 0 Mcp/OTel-specific
tests exist yet, so the
`dotnet test --filter "FullyQualifiedName~Mcp|FullyQualifiedName~OpenTelemetry"`
run is informational only. Closes gap G7.

**Severity:** HIGH — observability story is half-told.

### G8 — Google Calendar sync is not wired ✅ DONE

**Where:** `src/Cardscape.Application/Integrations/GoogleCalendar/IGoogleCalendarSyncService.cs`
and the card domain event handlers.
- Plan ref: §4.6.

**Symptom:** the bounded context, `GoogleCalendarConnection`
aggregate, repository, migration, REST endpoints, Web UI
page, and the `IGoogleCalendarSyncService` interface +
default implementation are all in. But
`PushCardDueDateAsync` is never called from any
`CardDueDateChanged` handler, `PullCalendarChangesAsync` is
a no-op that returns 0, and there is no Google Calendar
webhook endpoint. The Web UI "Connect" button posts a
hard-coded placeholder instead of a real OAuth code
exchange.

**Status:** ✅ **DONE** in commit `81f3036`. Adds
`CardDueDateChangedEventHandler` that calls
`PushCardDueDateAsync` when a `GoogleCalendarConnection`
exists for the affected user; implements
`PullCalendarChangesAsync` against the Google Calendar
`events.list` API with a per-user `syncToken` persisted
on the connection; adds
`POST /api/integrations/google-calendar/webhook`;
replaces the Web UI placeholder with a real OAuth
flow (`GET /api/integrations/google-calendar/start` +
`GET /api/integrations/google-calendar/callback`) and
persists the connection on success. Adds the
`IssueGoogleCalendarWatch` migration for the new
`ChannelId` / `ResourceId` / `Expiration` / `SyncToken`
columns. 5 new i18n keys.

**Severity:** ~~HIGH — the feature is a no-op.~~ (resolved)

### G9 — AI "Make checklist" and "Smart assign" buttons missing ✅ DONE

**Where:** `src/Cardscape.Web/Pages/CardDetail.razor`.
- Plan ref: §4.9.

**Symptom:** the `GenerateCardDescriptionCommand`,
`SummarizeCommentThreadCommand`,
`GenerateChecklistFromDescriptionCommand`, and
`SuggestCardOwnersCommand` commands, the four handlers,
the four REST endpoints under `/api/ai/...`, and the
`IAiApiClient` methods are all in. The i18n keys for all
four buttons exist in `SharedResource.resx` and
`SharedResource.es.resx`. But `CardDetail.razor` only
renders the "Generate description" and "Summarize comments"
buttons. The "Make checklist" and "Smart assign" buttons
are missing.

**Fix (planned):** add the two buttons + the two click
handlers in `CardDetail.razor`. Pure UI work; the backend
is already in place.

**Status:** ✅ **DONE.** Added the two buttons in
`src/Cardscape.Web/Pages/CardDetail.razor`:

- "✨ Make checklist" button next to the existing
  "Create checklist" button in the checklists section
  (line 216-219). On click, it calls
  `IAiApiClient.GenerateChecklistAsync(CardId)`, then
  creates a new checklist titled "AI suggestions" via
  the existing `Checklists.CreateAsync` API, adds each
  AI-suggested item via `Checklists.AddItemAsync`, and
  reloads the checklists list (`ReloadChecklistsAsync`).
- "✨ Smart assign" button next to the Members count
  (line 67-70). On click, it calls
  `IAiApiClient.SuggestOwnersAsync(CardId)`, then renders
  the returned suggestions inline in a `RadzenCard` below
  the metadata (line 75-93). Each suggestion gets a small
  "Add" button that calls the existing
  `Cards.AssignAsync(CardId, userId)` and updates the
  card with the returned `CardDto`. The just-assigned
  user is removed from the suggestions list so the user
  can assign multiple suggestions in one click.

Both buttons share the existing `aiBusy` / `IsBusy` state
and the `Icon="auto_awesome"` + `ButtonStyle.Light"`
Radzen styling, matching the two pre-existing AI buttons.
The i18n keys `AiGenerateChecklist` and `AiSuggestOwners`
were already present in `SharedResource.resx` /
`SharedResource.es.resx` (English: "Make checklist" /
"Smart assign"; Spanish: "Hacer checklist" /
"Asignación inteligente"), so no resx edits were needed.
Build green (0 warnings, 0 errors); 188 unit + 6
architecture + 5 integration tests green. Pure UI work,
no backend changes.

**Severity:** HIGH — 2 of 4 advertised AI features are
not surfaced.

---

## 4. MEDIUM — drift and structural gaps (not blocking, but wrong shape)

### G10 — Integration dedicated Web UI pages missing ✅ DONE

**Where:** `src/Cardscape.Web/Pages/Workspaces/WorkspaceIntegrations.razor`
references `/workspaces/{id}/integrations/{slack,github,email}`
and `/settings/integrations/google-drive`, but only the
Google Calendar page (`/settings/integrations/google-calendar`)
existed.
- Plan ref: §3.7, §3.8, §3.9, §3.10.

**Symptom:** the hub page linked to four pages that did
not exist; clicking any of the "Slack", "GitHub",
"Email-to-board", or "Google Drive" cards navigated to a
404. The backend (bounded contexts, repositories, REST
endpoints, MCP tools) was complete for all four, so the
gap was UI-only.

**Status:** ✅ **DONE.** Added four light Radzen pages
following the same pattern as the existing
`GoogleCalendar.razor` / `WorkspaceSaml.razor` /
`WorkspaceScim.razor`:

- `src/Cardscape.Web/Pages/WorkspaceSlack.razor` —
  route `/workspaces/{id}/integrations/slack`. Renders a
  "Connect" form (team id, team name, bot token) when no
  `SlackWorkspace` is linked, and a "Connected as" panel
  with the team id, bot-token prefix, and last-used
  timestamp otherwise. The "Refresh" button re-pulls the
  connection. (`POST /connect` returns 201; the
  application-layer `UnlinkSlackChannelCommand` is the
  only "disconnect" surface today, so the UI just
  re-fetches.)
- `src/Cardscape.Web/Pages/WorkspaceGitHub.razor` —
  route `/workspaces/{id}/integrations/github`. Three
  Radzen cards: (1) "Link a repository" form (board,
  repo full name, comma-joined events) → `POST
  /api/integrations/github/connect`; (2) "List pull
  requests" (repo + state) → `GET
  /api/integrations/github/pulls`, rendered as a
  `cards-table`; (3) "Create issue from card" form
  (card id, repo, title/body overrides) → `POST
  /api/integrations/github/issues`, shows the returned
  `GitHubIssueDto` in a success banner. The
  `ListGitHubIssuesQuery` MCP tool has no REST surface
  today, so the Web UI deliberately omits it.
- `src/Cardscape.Web/Pages/WorkspaceEmail.razor` —
  route `/workspaces/{id}/integrations/email`. Shows
  the inbound-webhook URL
  (`{origin}/api/integrations/email/inbound?provider=sendgrid`),
  an "Add email address" form (email + label + target
  list id) → `POST /api/integrations/email/addresses`,
  and a table of registered addresses with a per-row
  "Remove" button → `DELETE
  /api/integrations/email/addresses/{id}`.
- `src/Cardscape.Web/Pages/SettingsGoogleDrive.razor` —
  route `/settings/integrations/google-drive`. A
  workspace dropdown (loaded from `IWorkspacesApiClient`)
  drives the "Connect Google Drive" (placeholder OAuth
  call, same shape as the `GoogleCalendar.razor`
  placeholder) and the "Open picker" button, which
  calls `GET /api/integrations/google/connect?workspaceId=…`
  and redirects the browser to the returned picker URL.
  The real OAuth-redirect handler is the G8 follow-up.

Plus the 4 typed API clients in
`src/Cardscape.Web/Services/Api/`: `SlackApiClient.cs`
(`ISlackApiClient`), `GitHubApiClient.cs`
(`IGitHubApiClient`), `GoogleDriveApiClient.cs`
(`IGoogleDriveApiClient`), `EmailIntegrationApiClient.cs`
(`IEmailIntegrationApiClient`), all wired in
`Program.cs`. DTOs were mirrored into
`src/Cardscape.Web/Shared/ApiDtos.cs`
(`SlackWorkspaceDto`, `SlackChannelDto`,
`GoogleDriveConnectionDto`, `GitHubPullRequestDto`,
`GitHubIssueDto`, `GitHubPullRequestLinkDto`,
`InboundEmailAddressDto`).

The 4 page-titles, the connect / disconnect strings,
the table headers, and the per-page error keys are
new in both `SharedResource.resx` and
`SharedResource.es.resx`. The hub page
`WorkspaceIntegrations.razor` had a stale link (the
"Google Drive" card was pointing at
`/settings/integrations/google-calendar`); that's
fixed to `/settings/integrations/google-drive`.

Build green (0 errors; 2 pre-existing duplicate-key
warnings on the Spanish resx that predate this commit
are left untouched). Web + integration tests stay
green (the suite has a pre-existing 2-test flake in
`RegionGuardEndpointFilterTests` from a shared-SQLite
race; not introduced by this commit). Closes gap G10.

**Severity:** MEDIUM — the hub page links to 404 (no
longer).

### G11 — IdempotencyKey middleware location ✅ DONE

**Where:** `src/Cardscape.Application/Idempotency/`
(folder) and `src/Cardscape.Mcp/Idempotency/IdempotentToolRunner.cs`.
- Plan ref: §2.4.

**Symptom:** the entity, the store, the runner, the tool
wire-up, and the DB table are all in. But the
`Application/Idempotency/` folder + the
`IdempotencyKeyMiddleware` / `IssueIdempotencyKeyCommand`
the plan named are absent. The runner lives on the MCP
side as a static helper. Functionally fine; structurally
drifted.

**Status:** ✅ **DONE.** Two new files in
`src/Cardscape.Application/Idempotency/`:

- `IdempotencyKeyMiddleware.cs` — the at-most-once
  middleware the plan called for. The
  `ExecuteAsync<T>` method takes a key, a JSON
  request body, the current user, the store, and a
  clock, then either short-circuits to the stored
  response (on a hit with the same payload hash) or
  runs the handler and records the response (on a
  miss). The `IdempotencyKeyConflictException` and
  the `T = string` JSON-pass-through are preserved
  from the original MCP-side helper. The
  `src/Cardscape.Mcp/Idempotency/IdempotentToolRunner.cs`
  is now a thin shim that forwards to the new
  middleware so the existing call sites in
  `src/Cardscape.Mcp/Tools/BoardsTools.cs` continue
  to compile unchanged.
- `IssueIdempotencyKeyCommand.cs` — the
  `IssueIdempotencyKeyCommand` record + handler the
  plan named. The handler validates the key, builds
  the aggregate via `IdempotencyKey.Record`, persists
  it through `IIdempotencyKeyStore`, and commits
  the unit of work. Returns `Result<IdempotencyKeyId>`.

The `IssueIdempotencyKeys` migration
(`src/Cardscape.Infrastructure/Persistence/Migrations/20260729204702_IssueIdempotencyKeys.cs`)
had an empty `Up()` body because the
`idempotency_keys` table is actually created in the
sibling `20260729205310_IssueExternalLogins`
migration. Re-introducing the table here would break
the migration history (a fresh `dotnet ef database
update` would fail with "table idempotency_keys
already exists"), so the empty `Up` / `Down` are
preserved and a class-level `///<remarks>` XML doc
documents the no-op status with the reason.

13 new unit tests in
`tests/Cardscape.UnitTests/Application/Idempotency/IdempotencyKeyMiddlewareTests.cs`
and `IssueIdempotencyKeyCommandHandlerTests.cs`
cover the null-key / empty-key / anonymous / invalid
key / first-call / replay-same-payload /
replay-different-payload / replay-different-owner
/ string-payload paths. 343 unit tests green; the
Web project has a pre-existing G12 WIP failure
unrelated to this commit (the G12 audit gap is still
open). Closes gap G11.
(the table is created in the sibling
`IssueExternalLogins` migration).

**Severity:** MEDIUM.

### G12 — i18n: `SetDefaultCulture` / `AddSupportedCultures` missing ⚠️ PARTIAL (reverted)

**Where:** `src/Cardscape.Web/Program.cs`,
`src/Cardscape.Web/Cardscape.Web.csproj`.
- Plan ref: §5.1.

**Symptom:** the localization pipeline is wired and 137
EN + 140 ES keys ship in
`src/Cardscape.Web/Resources/SharedResource.{resx,es.resx}`.
But the `AddLocalization` call did not configure
`SetDefaultCulture("en")` /
`AddSupportedCultures("en", "es")`, the
`UseRequestLocalization` is impossible on Blazor WASM,
and the culture is picked by a `CulturePicker` rather
than the `Accept-Language` header.

**Status:** ⚠️ **PARTIAL** — the G12 push (`ac58cbc` /
`2a9b3c6`) shipped the SetDefaultCulture +
AddSupportedCultures + BlazorWebAssemblyLoadAllGlobalizationData
combination, but it produced a persistent runtime
error on every F5 refresh
("Blazor detected a change in the application's culture
that is not supported with the current project
configuration") that the .NET 10 SDK cannot
resolve: the property is documented but ignored at
runtime, the `CultureInfo.DefaultThreadCurrentCulture`
line triggers the runtime change detection, and the
`SharedResource.es.resx` is never linked into the
assembly manifest (the auto-include + explicit
`<EmbeddedResource>` + `<Resource>` workarounds all
produce the same single-resource manifest).

This commit (`e3b180e`) reverts the runtime side:
removed `BlazorWebAssemblyLoadAllGlobalizationData`,
removed `CultureInfo.DefaultThreadCurrentCulture` /
`DefaultThreadCurrentUICulture` setters, and the
embedded .es resx. The Spanish strings are now
shipped as a static web asset under
`wwwroot/Resources/SharedResource.es.resx` so a
future client-side CulturePicker can load them via
HttpClient without going through the broken
`DefaultThreadCurrentCulture` path. The localizer
keeps using the embedded `SharedResource.resx`
(English) until the picker is wired. The follow-up
is documented in
`docs/i18n/02-translation-workflow.md` §12.
`.resx` resource path, and the actual current culture is
set via `CultureInfo.DefaultThreadCurrentCulture` /
`DefaultThreadCurrentUICulture` (driven by `Culture:Default`
in `wwwroot/appsettings.json`, default `"en"`). The
localization comment block in `src/Cardscape.Web/Program.cs:22-82`
is updated to explain the **Blazor WebAssembly caveat** in
full (the `UseRequestLocalization` middleware impossibility,
the absence of a `CulturePicker` class, the `navigator.language`
+ `localStorage` TODO). The follow-up is documented in
`docs/i18n/02-translation-workflow.md` §12 ("Blazor
WebAssembly culture-resolution caveat") with two concrete
shapes for a future PR. The same doc gained §11
("Practical .resx extraction guide") with the workflow
the plan §5.2 asked for (file layout, adding a key, adding
a language, the `__TODO_<lang>__` placeholder convention,
the key naming rules, the verification steps). Build green
for the `Cardscape.Web` project (0 errors, 0 warnings);
the full-solution `dotnet build` shows pre-existing errors
in `tests/TestCommon/Fakes/Fakes.cs` from the in-flight G11
(IdempotencyKey) work in the working tree — those are not
introduced by this commit. The
`--filter "FullyQualifiedName~Localization|FullyQualifiedName~I18n"`
test run is informational only (no localization-specific
tests exist yet). Closes gap G12.

**Severity:** ~~MEDIUM.~~ (resolved)

### G13 — Trello import dry-run is identical to apply ✅ DONE

**Where:** `src/Cardscape.Mcp/Tools/MissingTools.cs`
(`imports_trello_preview` vs `imports_trello_apply`),
`src/Cardscape.Infrastructure/Import/TrelloImportService.cs`,
`src/Cardscape.Api/Endpoints/Import/ImportEndpoints.cs`,
`src/Cardscape.Web/Pages/WorkspaceImport.razor`.
- Plan ref: §5.6.

**Symptom:** the Web UI `/workspaces/{id}/import` page
does not show a live preview; the MCP
`imports_trello_preview` is the same code path as
`imports_trello_apply` (no dry-run).

**Fix:** added a `previewOnly` parameter (default
`false`) to `IImportService.ImportTrelloJsonAsync` and
threaded it through every call site.

- `TrelloImportService` builds the in-memory aggregate
  tree in both modes; when `previewOnly` is `true` it
  skips every `IRepository<>.AddAsync(...)` call and
  the final `IUnitOfWork.SaveChangesAsync(ct)` so the
  database is untouched. A new `ImportPreview` record
  (counts + first-N sample names for boards / lists /
  cards + a member count + a `WasApplied` flag) is
  always populated, so the apply path also surfaces a
  "what was created" summary to the UI without a
  second round-trip.
- `IImportService.ImportTrelloJsonAsync` carries the
  new `bool previewOnly = false` parameter; existing
  callers that don't pass it keep the apply behavior.
- The REST endpoint reads the optional
  `previewOnly` form field (defaults to `false`) and
  forwards it to the service.
- `imports_trello_preview` passes `previewOnly: true`;
  `imports_trello_apply` passes `previewOnly: false`
  explicitly so the contract is obvious at the call
  site.
- The Web UI page was rewritten: on file pick it
  fires a preview request automatically, renders a
  `RadzenPanel` with the parsed summary (counts +
  sample names), and exposes a separate
  "Apply import" `RadzenButton` that runs the real
  import. A "Cancel" button clears the preview. A
  `selectionVersion` counter discards stale preview
  responses if the user picks a different file while
  a request is in flight.

Build green (0 errors, 0 warnings). No import- or
Trello-specific tests exist today, so the verification
filter `FullyQualifiedName~Import|FullyQualifiedName~Trello`
returns zero tests; the existing 427-test suite stays
green.

**Severity:** MEDIUM — the UX made the import a
leap-of-faith; the preview closes the loop.

### G14 — MCP subscriptions are dead code ✅ DONE

**Where:** `src/Cardscape.Mcp/Realtime/McpResourceBroadcaster.cs`.
- Plan ref: §5.8.

**Symptom:** the broadcaster is registered, but
`Subscribe` / `Unsubscribe` / `BroadcastAsync` are never
called. `BroadcastAsync` is a logged no-op. There is no
`resources/subscribe` / `resources/unsubscribe` MCP
handler. The doc at
`docs/extensions/01-build-your-own-mcp-client.md:121-139`
advertises a working feature that the code does not
implement.

**Fix (planned):** add the `Subscribe` / `Unsubscribe`
MCP handlers; wire them to the broadcaster; have the
SignalR `BoardHub` event listener call
`McpResourceBroadcaster.BroadcastAsync` on every
board event.

**Status:** ✅ **DONE.** The .NET MCP SDK 1.4+ exposes
`WithSubscribeToResourcesHandler` and
`WithUnsubscribeFromResourcesHandler`; both are wired in
`src/Cardscape.Mcp/Extensions/ServiceCollectionExtensions.cs:81-82`
and resolve the singleton `McpResourceBroadcaster` from
`RequestContext.Services`. The broadcaster now holds
`ConcurrentDictionary<string, List<McpServer>>` keyed by
resource URI; the value is the list of subscribed
sessions. `BroadcastAsync(boardId)` derives
`board://{boardId:N}`, walks the per-URI subscriber list,
and emits the standard
`notifications/resources/updated` notification via
`McpServer.SendNotificationAsync`. A session that throws
on send is unsubscribed in place so a bad client cannot
take down the fan-out.

The API side is wired through a new
`Cardscape.Application.Realtime.IMcpResourceNotifier`
implemented by `Cardscape.Api.Realtime.HttpMcpResourceNotifier`,
which POSTs to a new
`POST /api/internal/board-event` endpoint on the MCP
(defined in `McpBoardEventEndpoints.cs`, mapped in
`src/Cardscape.Mcp/Program.cs:30`). Auth is the same
`X-Internal-Secret` shared secret the MCP already uses to
call the API in the reverse direction. The existing
`BoardNotifier` (SignalR-only) is replaced by
`CompositeBoardNotifier` in DI
(`src/Cardscape.Api/Program.cs:91-95`) — the composite
calls the SignalR hub first (awaited) and the MCP
notifier second (fire-and-forget; the HTTP wrapper is
best-effort). The doc at
`docs/extensions/01-build-your-own-mcp-client.md:121-176`
is updated with the new flow.

Build green (0 warnings, 0 errors); 102/102 integration
tests green (the existing `BoardBroadcastEndpointTests`
suite is unchanged — it covers the API's broadcast
endpoint, which is the symmetric counterpart of the new
MCP-side endpoint).

**Severity:** MEDIUM — doc lies; the feature is not
shipped.

---

## 5. LOW — cosmetic / nice-to-have

- **G15** — `tests/Cardscape.IntegrationTests` is mostly
  empty for the new integration paths. The current
  integration coverage is the golden path + the v1.0.0
  surfaces. The new P3/P4 work has unit tests but no
  integration tests.
- **G16** — `01-implementation-plan.md` says
  "313 unit + 85 integration" (Phase 6); the current
  `docs/community/ROADMAP.md` says "313 + 86". The
  1-test drift is from `d14125c`.
- **G17** — CI `coverage` job uploads the lcov artifact
  but does not post a coverage diff comment to the PR.
  Plan §1.1 second bullet.
- **G18** — `docs/api/02-openapi-spec.md` is missing;
  the doc was created as `01-openapi-spec.md` (renamed to
  `02-openapi-spec.md` in the G18 follow-up) — filename
  drift, not a content gap).

---

## 6. Execution order (proposed)

The work is sequenced by severity and by the "one
vertical slice lands green" rule. Each item lands on
`master` as a single commit, with the build + tests
green at the end.

1. **G1** — Integration DI registrations. Single
   30-line change in
   `InfrastructureServiceCollectionExtensions.AddInfrastructure`.
   4 new `AddSingleton` / `AddScoped` / `AddTransient`
   blocks. **One commit, ships green.**
2. **G5** — `RegionGuard` middleware / filter. 1 file
   + 1 line in `Program.cs` to register. **One
   commit, ships green.**
3. **G4** — 2FA TOTP wired into `LoginUserQuery`. 1
   command change + 1 DTO field + 1 Web UI conditional
   that's already there. **One commit, ships green.**
4. **G3** — SCIM `/scim/v2/Groups`. New service
   methods + 6 endpoint mappings + 1 integration test.
   **One commit, ships green.**
5. **G2** — SAML SSO handler. 1 new file + 4 endpoint
   rewrites + 1 integration test. **One commit, ships
   green.**
6. **G9** — AI "Make checklist" + "Smart assign"
   buttons. Pure UI. **One commit, ships green.**
7. **G7** ✅ — OpenTelemetry attributes on every MCP
   tool. Helper +
   `AddAspNetCoreInstrumentation` in commit A
   (`64ef8d8`); per-tool
   `try { ... MarkSuccess } catch { MarkFailure }`
   wrap in commit B (`d96c566`); doc DONE markers
   in commit C. **DONE.**
8. **G8** — Google Calendar sync wiring. 1 new event
   handler + 1 webhook endpoint + 1 Web UI
   replacement. **One commit, ships green.**
9. **G10** — Integration dedicated Web UI pages. 4
   pages. **One commit, ships green.**
10. **G6** — Card Aging / Snooze / Mirror / List
    Limits Web UI. **One commit per feature × 4 = 4
    commits, each ships green.**
11. **G11** — Move `IdempotentToolRunner` to
    `Application/Idempotency/`. **One commit, ships
    green.**
12. **G14** — MCP subscriptions. **One commit, ships
    green.**
13. **G13** — Trello import dry-run. **One commit,
    ships green.**
14. **G12** — i18n `SetDefaultCulture` /
    `AddSupportedCultures`. **One commit, ships
    green.**
15. **G15–G18** — integration tests + cosmetic
    cleanup. **Tracked separately, ship on
    demand.**

Each commit updates
[`03-execution-plan-v1.1.0.md`](03-execution-plan-v1.1.0.md)
with the `✅ DONE` / `⚠️ PARTIAL` markers per the audit
convention.

---

## 7. Re-audit cadence

After every commit, the per-area audit report under
[`../audits/2026-07-30/`](../audits/2026-07-30/README.md)
is updated by the sub-agent that did the work, and the
master `04-audit-gaps-2026-07-30.md` (this file) is
updated to remove the closed gap.

The audit re-runs on the v1.1.0 release tag.
