chore(release): v0.3.0-api-tokens artifacts (README + CHANGELOG)

Phase 3 done. Bump the README status table to mark v0.3.0 done
and re-frame Phase 4 as the collaboration/extensions/automation
wave. Add the v0.3.0 entry to CHANGELOG.md with the Added /
Changed / Removed / Security breakdown that matches the project
convention.

No code changes; the test count is the same as on master
(187 unit + 20 integration). The tag will follow in a
separate commit so the commit graph reads naturally.
